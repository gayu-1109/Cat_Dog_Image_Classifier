# Cat_Dog_Image_Classifier
A Convolutional Neural Network (CNN) based web application built using TensorFlow/Keras and Flask that classifies uploaded images as either a Dog or a Cat. The project includes model training, image preprocessing, and a user-friendly web interface for real-time predictions with uploaded image preview support.
